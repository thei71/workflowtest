name: workflow-test

on:
  workflow_dispatch:
    secrets:
      MYSECRET:
        required: true

jobs:
  testjob:
    runs-on: [ linux, x64 ]
    steps:
      - run: echo ${{ secrets.MYSECRET }} > testfile.secret && cat testfile.secret && rm -f testfile.secret
